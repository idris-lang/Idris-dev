universes001.idr:7:5:Universe inconsistency.
        Working on: x
        Old domain: (5,5)
        New domain: (5,4)
        Involved constraints: 
                ConstraintFC {uconstraint = x <= z, ufc = universes001.idr:7:5}
                ConstraintFC {uconstraint = x < y, ufc = universes001.idr:1:6}
                ConstraintFC {uconstraint = z < x, ufc = universes001.idr:1:6}
                ConstraintFC {uconstraint = x <= z, ufc = universes001.idr:7:5}
